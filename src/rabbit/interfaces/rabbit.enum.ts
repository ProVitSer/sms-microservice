export enum QueueTypes {
    sms = 'sms',
}

export enum RabbitMqExchange {
    presence = 'presence',
}

export enum RoutingKey {
    sendSms = 'send.sms',
}

export enum Channels {
    sms = 'sms',
}
