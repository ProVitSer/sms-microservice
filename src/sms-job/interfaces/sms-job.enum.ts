export enum SmsJobStatus {
    apiFail = 'apiFail',
    inProgress = 'inProgress',
    completed = 'completed',
    planned = 'planned',
}

export enum SmsJobSendStatus {
    error = 'error',
    inProgress = 'inProgress',
    completed = 'completed',
}
